# Operations quicknote

Use `run_pipeline.py --dry-run` for the default dry run. After human approval, rerun without `--dry-run` to generate PDFs, compose emails, and hand off to Gmail helpers. Keep secrets in `.env` locally and GitHub Secrets in CI.
